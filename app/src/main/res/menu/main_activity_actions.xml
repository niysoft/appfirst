<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dailybela="http://schemas.android.com/apk/res-auto">
    <!-- Search, should appear as action button -->
   <!-- <item
        android:id="@+id/toggBtn"
        android:icon="@drawable/swicth"
        android:title="@string/action_search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        dailybela:showAsAction="always"
        />-->

   <!-- <item
        android:id="@+id/mobile"
        android:icon="@drawable/ic_action_search"
        android:title="Setting"
        dailybela:showAsAction="ifRoom|withText"
        />

    <item
        android:id="@+id/search"
        android:title="Terms"
        android:icon="@drawable/ic_action_search"
        dailybela:showAsAction="ifRoom|collapseActionView"
        android:actionLayout="@layout/search_layout"
        />-->
</menu>